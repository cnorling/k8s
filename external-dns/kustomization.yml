apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: external-dns

namespace: external-dns

resources:
  - clusterrole.yml
  - clusterrolebinding.yml
  - deployment.yml
  - namespace.yml
  - secret.yml
  - service.yml
  - serviceaccount.yml
